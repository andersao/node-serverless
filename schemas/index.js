const post = require('./post-schema');
const product = require('./product-schema');
const articles = require('./article-schema');
const articleBatches = require('./article-batch-schema');

module.exports = {
  post,
  product,
  articles,
  articleBatches
};
